"""Skeleton for styling/design-consistency checks across .wire pages.

Candidates: the feature-card gradient palette CLAUDE.md documents for
index.wire, `<style scoped>` blocks parsing/compiling cleanly, Pico CSS
class usage conventions. Not implemented yet.
"""

import pytest

pytestmark = pytest.mark.web


def test_placeholder():
    pytest.skip("Styling test harness not implemented yet — see module docstring")
